class Solution {
public:
    int findPeakElement(vector<int>& nums) {

       sort(nums.begin(),nums.end());

       set<int> s;

       for(auto it:nums){
    s.insert(it);
       }

       return
        
    }
};
